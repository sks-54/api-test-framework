# === Spec-required packages (from assignment spec) ===
pytest>=7.4,<9.0
requests>=2.31,<3.0
allure-pytest>=2.13,<3.0
pyyaml>=6.0,<7.0

# === Framework additions (beyond spec, documented in ENHANCEMENTS.md) ===
pdfplumber>=0.10,<1.0   # PDF spec parser (spec_parser/pdf_parser.py)
pytest-retry>=1.0,<2.0  # Retry on 5xx responses (reliability test coverage)
